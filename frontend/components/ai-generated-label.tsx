import { cn } from '@/lib/utils';
import { Sparkles } from 'lucide-react';
import { Tooltip, TooltipContent, TooltipTrigger } from './ui/tooltip';

export interface AiGeneratedLabelProps {
  className?: string;
}

export function AiGeneratedLabel({ className }: AiGeneratedLabelProps) {
  return (
    <Tooltip>
      <TooltipTrigger asChild>
        <div
          className={cn(
            'inline-flex items-center gap-1 text-xs bg-blue-100 text-blue-800 rounded-md px-2 py-1 font-medium',
            className,
          )}
        >
          <Sparkles className="w-3 h-3" />
          <span className="text-xs">AI Generated</span>
        </div>
      </TooltipTrigger>
      <TooltipContent>
        <p>
          This content was generated by an AI model and can contain mistakes. <br /> Please verify the content before
          using it.
        </p>
      </TooltipContent>
    </Tooltip>
  );
}
